栈中top-1,top+1和top--,top++,--top,++top有区别，前者仅用于查看，
而后者用于弹出栈顶元素，因为会修改指针，你要查看栈顶元素，那就*(S.top-1)，
而*(--S.top)不行（亲测）

循环链表再队尾增删操作时，不要先改尾节点指针域指向，不然你就无法通过尾节点找到头节点了

队列的定义：是通过为队列基地址Q.base来new一个MAXSIZE大小的数组来完成,front和rear作为下标，这样就可以
通过Q.base[Q.rear]/Q.base[Q.front]等来表示队列元素

队列循环是通过将下标rear或者front（具体看定义方式）的更新方式改为Q.rear=(Q.rear+1)%MAXSIZE类似来实现

假设无队头下标，队尾下标rear指向队尾元素位置，队列元素个数为length，
则队头元素位置=(rear-length+1+MAXSIZE)%MAXSIZE
分析：正常情况下（rear在队头元素后面时，易知队头元素位置=(rear-length+1)，但是因为是循环队列，
需要处理rear-length+1为负数的情况，就加MAXSIZE再对MAXSIZE取余，这样就能得到队头元素位置，
这对一般情况也同样适用

定义动态数组的一个思路（手动扩容）：    
    初始时,分配一个固定大小的数组(比如n=10)
    当数组快要满时（i>=n-1），我们分配一个更大的数组（比如2倍大小）。
    将原数组的内容复制到新数组。
    释放原数组的内存。
    将指针指向新数组。

也可利用"#include<string>",用string来定义数组，可实现自动扩容，是实际编程中更常用的