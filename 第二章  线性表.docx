顺序表需要两个成员：数组和length长度
线性表结构：
    typedef struct{
        ElemType elem[MAXSIZE];     //SqList L; 每个元素：L.elem[i];
        int length;
    }SqList;        //静态数组

    typedef struct{
        ElemType *elem;  //指向数组头元素地址也代指整个数组       
        int length;     
    }SqList;    //SqList L; 每个元素：L.elem[i];
    L.elem=new ElemType[MAXSIZE];       //动态数组

    循环链表：最后一个节点的指针域指向头结点，形成一个环
    优点：从表中任意一个节点出发，都能找到表中的其他节点
    空循环链表：只有一个头结点，头指针和头结点指针域均指向这个节点，也就是说这两个指针相同，也就是说头结点的指针域
    存的是头指针
    正常单链表当指针域为空时我们就知道到达最后一个节点，而在循环链表中，判断是否到达最后一个节点
    是看这个节点的指针域是否等于头指针
    也就是说循环条件：p!=L;     //p不是头结点
                    p->next!=L; //p的指针域不等于头指针
    因为如果P=L，也就是说只有一个节点，不用循环
    如果要经常操作首尾节点，为了避免时间复杂度较大，通常通过尾指针去找，用尾指针去表示循环链表
    R为尾指针（指向尾结点），则第一个数据节点（头结点之后）a1存储位置=R->next->next,
    最后一个数据节点an的存储位置=R，这样不管找a1还是an，时间复杂度均为O(1)；
